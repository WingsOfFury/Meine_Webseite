---
import BaseLayout from "../layouts/BaseLayout.astro";
import Refresh from "../components/Refresh.jsx";
// import type { loadApp } from "astro/app/node";

const pageTitle = "Referenzen | Akansel Cengiz";
const description = "Stimmen aus der Community...";
---

<BaseLayout pageTitle={pageTitle} description={description}>
  <Refresh client:load />
</BaseLayout>
