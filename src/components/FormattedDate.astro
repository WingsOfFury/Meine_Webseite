---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toLocaleTimeString()}>
  {
    date.toLocaleDateString("de-de", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
