import React from 'react';

const Location = ({ city }) => {
    return (
        <section>
            <p>kommt aus der Stadt <b>{city}</b>.</p>
        </section>
    );
};

export default Location;
